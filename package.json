{
  "name": "domain-migrator",
  "version": "1.0.0",
  "description": "Autonomous domain transfer tool for migrating domains between registrars",
  "private": true,
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.32"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
